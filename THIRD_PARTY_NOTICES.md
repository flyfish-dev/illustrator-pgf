# Third-Party Notices

The published runtime contains no copied third-party parser source, Adobe SDK, Illustrator binary, font, ICC profile, test document, or bundled WASM module.

Node compression uses the Node.js standard library. Browser zstd is supplied by the host through the documented decoder interface; any adapter/WASM selected by the host must carry its own version and license notice.

External projects mentioned in research or the development specification are references only and are not copied into this package.
