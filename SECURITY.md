# Security Policy

Do not submit confidential Illustrator files to public issues. Report parser crashes, unbounded memory/CPU, SVG injection, unexpected network access, path access, or Worker cancellation failures privately to the repository maintainer.

Supported security fixes currently target the latest development release. Every input is treated as hostile; see `docs/security.md` for the parser threat model and limits.
