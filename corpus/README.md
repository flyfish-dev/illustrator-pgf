# Corpus

`synthetic/` contains only project-authored fixtures. They validate parser mechanics but cannot establish real Illustrator version compatibility. `manifests/` records provenance and expected features. Authorized real files and commercial resources must remain outside the public repository unless redistribution is explicitly granted.
